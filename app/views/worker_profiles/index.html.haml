%h1 Listing worker_profiles

%table
  %tr
    %th
    %th
    %th

  - @worker_profiles.each do |worker_profile|
    %tr
      %td= link_to 'Show', worker_profile
      %td= link_to 'Edit', edit_worker_profile_path(worker_profile)
      %td= link_to 'Destroy', worker_profile, :method => :delete, :data => { :confirm => 'Are you sure?' }

%br

= link_to 'New Worker profile', new_worker_profile_path
